<%= form_for Rating.new do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <%= f.text_area :content, size: Settings.rating_form_size, class: "form-control" %>
  <%= f.submit t(".post"), class: "submit-btn" %>
<% end %>
